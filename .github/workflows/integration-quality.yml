name: Integration Quality

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  integration-lint:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Check Home Assistant integration correctness
        run: uvx ruff check --select E4,E7,E9,F custom_components/speaker_recognition

      - name: Compile Home Assistant integration
        run: python -m compileall -q custom_components/speaker_recognition
